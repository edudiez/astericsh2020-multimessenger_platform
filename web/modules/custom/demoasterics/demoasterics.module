<?php

//* ASIGNACIO DIRECTA DE ROLE PROFESIONAL *//
function demoasterics_user_presave(Drupal\Core\Entity\EntityInterface $entity) {
    if($entity->isNew()) {
      $entity->addRole('profesional');
    }
}
